import { CustomTitleCasePipe } from './custom-title-case.pipe';

describe('CustomeTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
